import CartItemList from "./CartItemList";
import SideBar from "./SideBar";

const page = () => {
  return (
    <div>
      <CartItemList />
      <SideBar />
    </div>
  );
};

export default page;
